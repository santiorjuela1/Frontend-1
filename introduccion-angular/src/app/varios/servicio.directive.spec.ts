import { ServicioDirective } from './servicio.directive';

describe('ServicioDirective', () => {
  it('should create an instance', () => {
    const directive = new ServicioDirective();
    expect(directive).toBeTruthy();
  });
});
